const { formatUser, formatUsers } = require("./userDTO");
const { formatEvent, formatEvents } = require("./eventDTO");
const { formatParticipation, formatParticipations } = require("./participationDTO");

module.exports = {
  formatUser,
  formatUsers,
  formatEvent,
  formatEvents,
  formatParticipation,
  formatParticipations
};
